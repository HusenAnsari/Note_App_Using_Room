package com.husenansari.noteappusingroom.util

class AppConstants {
    companion object {
        const val DB_NAME = "note_db.db"
        const val TYPE = "type"
        const val DATA = "data"
        const val INSERT = "insert"
        const val UPDATE = "update"
    }
}